# Sobre este projeto

Site institucional em Flask para a KG Cruz Assessoria Contábil (escritório de contabilidade). Feito com Python + Flask no back-end, templates HTML (Jinja) e CSS puro no front-end.

## Estrutura

- `app.py` — define as rotas do site (`/`, `/servicos`, `/sobre`, `/contato`), cada uma ligada a um arquivo em `templates/`
- `templates/` — um arquivo `.html` por página
- `static/css/style.css` — estilo visual do site
- `static/images/` — logo, marca d'água, foto do escritório e ícone do WhatsApp

## Estado atual

- `index.html`, `servicos.html`, `sobre.html` e `contato.html` — conteúdo completo
- Não existe mais página de Blog (removida a pedido do dono do site)

## Como eu quero trabalhar (importante)

- Sou iniciante em programação. Ainda estou aprendendo Python, Flask, HTML e CSS.
- Meu objetivo é entender cada passo do código, não só ter a tarefa concluída. Quero, em um futuro próximo, conseguir programar sem depender de IA.
- Antes de fazer qualquer mudança no código, explique o que vai fazer e por quê, em linguagem simples.
- Quando eu mandar um código com erro, não corrija de primeira — me faça perguntas que me ajudem a encontrar o problema sozinho. Só dê a resposta pronta se eu pedir explicitamente.
- Sempre que usar um termo técnico que eu talvez não conheça (ex: "framework", "endpoint", "decorator"), explique o significado antes de continuar.
- Prefira soluções simples e claras a soluções "espertas" ou avançadas demais para o meu nível atual.
